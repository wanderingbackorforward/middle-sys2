server.port=8080
spring.application.name=tunnel-monitor
server.servlet.context-path=/api
spring.jackson.serialization.write-dates-as-timestamps=false

# 生产环境日志配置
logging.level.root=INFO
logging.level.com.example.tunnel=DEBUG

# Supabase 配置
supabase.url=${SUPABASE_URL}
supabase.anon-key=${SUPABASE_ANON_KEY}
```

**改动**:
- 取消注释第15-16行
- 将 `supabase.key` 改为 `supabase.anon-key` (这是标准命名)

---

## 🔧 **修改步骤**

访问: https://github.com/wanderingbackorforward/middle-sys2/blob/master/backend/src/main/resources/application-prod.properties

1. 点击编辑按钮（铅笔图标）
2. 找到第15-16行 (Supabase 配置部分)
3. 删除这两行前面的 `#` 符号，使其变为：
```
   supabase.url=${SUPABASE_URL}
   supabase.anon-key=${SUPABASE_ANON_KEY}
